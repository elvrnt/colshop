services:
  backend:
    image: lavro/gateway-backend:a1b2c3d

    environment:
      SECRET_KEY: staging-secret
      DEBUG: "True"

      DB_ENGINE: django.db.backends.postgresql
      DB_NAME: kursach
      DB_USER: kursach
      DB_PASSWORD: kursach
      DB_HOST: db
      DB_PORT: 5432

      REDIS_HOST: redis
      REDIS_PORT: 6379